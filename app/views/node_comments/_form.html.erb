<%= form_for(@node_comment) do |f| %>
  <% if @node_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_comment.errors.count, "error") %> prohibited this node_comment from being saved:</h2>

      <ul>
      <% @node_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :node_id %><br />
    <%= f.number_field :node_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br />
    <%= f.number_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
